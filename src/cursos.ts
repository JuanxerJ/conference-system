export class Curso {
  public name: string;
  public code: string;
  public url: string;
  constructor(name: string, code: string, url: string) {
    this.name = name;
    this.code = code;
    this.url = url;
  }
}
